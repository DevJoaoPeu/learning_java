services:
  postgres:
    image: postgres
    container_name: learning_java
    environment:
      POSTGRES_PASSWORD: password
      POSTGRES_USER: postgres
      POSTGRES_DB: learning_java_db
    ports:
      - '5432:5432'
    volumes:
      - './.postgres-data:/var/data/lib/postgresql/data'